<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <link rel="stylesheet" href="/styles/build.css" />

    <title><%= title %></title>
  </head>
  <body>
    <div>
      <h1 class="text-cyan-600"><%= title %></h1>

      <form action="/new" method="post">
        <div>
          <label for="username">Author</label>
          <input
            type="text"
            name="username"
            id="username"
            value="<%= locals.data?.username %>"
            class="outline-0 border"
          />
          <% if (locals.errors?.username) { %>
          <span><%= errors.username.msg %></span>
          <% } %>
        </div>
        <div>
          <label for="text">Content</label>
          <textarea
            name="text"
            id="text"
            value="<%= locals.data?.text %>"
            rows="10"
            cols="30"
            class="outline-0 border"
          ></textarea>
          <% if (locals.errors?.text) { %>
          <span><%= errors.text.msg %></span>
          <% } %>
        </div>
        <div>
          <button type="submit">Create</button>
        </div>
      </form>
    </div>
  </body>
</html>
